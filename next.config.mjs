/** @type {import('next').NextConfig} */

const nextConfig = {
    basePath:'/valentines2025',
    assetPrefix:'/valentines2025',
    output: 'export',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
